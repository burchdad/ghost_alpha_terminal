<resources>

    <!-- Minimal base theme required by Android -->
    <style name="Theme.GhostAlpha" parent="Theme.DeviceDefault.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

</resources>
